

getline
perror (just a placeholder for now—we’ll discuss error handling soon)

strtok/strtok_r
strsep
strcpy
atoi
strpbrk
strcmp

mmap (this one requires a new system call)
malloc (Keep it simple! See example below)



Move to stdlib

strlen
memcpy
memset
read (a wrapper around the syscall invocation)
write (a wrapper around the syscall invocation)
printf
